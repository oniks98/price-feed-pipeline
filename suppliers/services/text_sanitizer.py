"""
Сервіс очищення тексту від небажаного вмісту у Prom-фіді.

Механізми (застосовуються в цьому порядку, див. TextSanitizer.sanitize):
  0. Нормалізація non-breaking space (U+00A0 → звичайний пробіл) — для ВСІХ
     постачальників, щоб literal-фрази LP/Viatec збігалися незалежно від того,
     який пробіл використало джерело.
  1. Supplier-специфічні видалення/заміни (LP, Viatec, Secur) — лише коли
     переданий відповідний `supplier`.
  2. _IMG_TAG_RE / _IFRAME_TAG_RE / _A_OPEN_TAG_RE+_A_CLOSE_TAG_RE / _MARKDOWN_LINK_RE — загальні
     для ВСІХ постачальників: видаляють <img>/<iframe>-теги цілком, видаляють
     відкриваючий/закриваючий <a>-тег окремо (працює і для незакритих/непарних <a>)
     та розгортають [текст](url), лишаючи тільки текст/вміст.
  3. _PROMO_ARTIFACTS      — видаляє промо-фрази/артефакти цілком (разом з роздільником).
  4. _HTML_ENTITY_DELETE_RE / _HTML_ENTITY_DASH_RE — для ВСІХ постачальників:
     прибирає "осиротілі" HTML-entity, що не входили цілком до жодної
     видаленої вище фрази. &nbsp,/&laquo,/тощо — видаляються повністю;
     &ndash/&mdash (візуальне тире-роздільник між словами) — замінюються
     на пробіл, щоб не злити сусідні слова/числа в одне.
  5. _GENERAL_REPLACE_RE   — термінологічні заміни окремих слів — для ВСІХ постачальників,
     case-insensitive, з збереженням регістру першої літери (напр. Шлюб → Брак).
  6. Viatec: видалення окремих слів (Viatec/Акція) — в самому кінці.
  7. BANNED_WORDS          — замінює заборонені слова пробілом (case-insensitive).
  8. Прибирання пробілу перед розділовими знаками — фіксує артефакти типу
     "менеджеру .", які залишаються після видалення слова перед крапкою.

Щоб додати заборонене слово        — додай до BANNED_WORDS.
Щоб додати промо-артефакт          — додай до _PROMO_ARTIFACTS.
Щоб додати загальне текстове правило
(для ВСІХ постачальників)          — додай до _GENERAL_REPLACE_RULES.
Щоб додати LP-правило              — додай до _LP_DELETE_PATTERNS (ручний regex) /
                                       _LP_DELETE_PHRASES (звичайний текст, escape'ується
                                       автоматично) / _LP_REPLACE_RULES.
Щоб додати Viatec-правило          — додай до _VIATEC_DELETE_PHRASES / _VIATEC_REPLACE_RULES.
Щоб додати Viatec-правило лише для описів
                                  — додай до _VIATEC_DESCRIPTION_REPLACE_RULES.
Щоб додати Secur-правило            — додай до _SECUR_DELETE_PHRASES (видалення фраз
                                       в sanitize(), для всіх SANITIZED_FIELDS) або
                                       підкоригуй _SECUR_NAME_QTY_PREFIX_RE (перенесення
                                       кількісного префікса "Набір/Набор N шт." в кінець
                                       назви — лише для TITLE_FIELDS, застосовується
                                       окремо в sanitize_item, див. _apply_secur_title_rules).
Щоб додати правило лише для ОПИСІВ
(Опис/Опис_укр, для ВСІХ постачальників)   — додай regex-правило поруч із
                                       _DESC_ONLY_COM_RE / _WARRANTY_PERIOD_RE та
                                       підключи виклик у _apply_desc_only_rules
                                       (застосовується ПІСЛЯ sanitize(), лише до
                                       TextSanitizer.DESCRIPTION_FIELDS — характеристик
                                       товару не зачіпає).

ВАЖЛИВО: supplier-специфічні правила застосовуються лише коли викликач
передав `supplier="lp"` / `supplier="viatec"` / `supplier="secur"` (див.
SupplierConfig.supplier_name). Без цього параметра supplier-специфічні
кроки просто пропускаються — це свідома ізоляція постачальників (жодних
побічних ефектів між ними).
"""

import re

# Слова, заборонені до розміщення на Prom.ua (у назві, описі, пошукових запитах).
# Перевірка case-insensitive, тому регістр тут не важливий.
BANNED_WORDS: list[str] = [
    "copy",
    "підробка",
    "подделка",
]

# Прекомпільований патерн для швидкості (будується один раз при імпорті)
_BANNED_PATTERN: re.Pattern = re.compile(
    r'\b(' + '|'.join(re.escape(w) for w in BANNED_WORDS) + r')\b',
    re.IGNORECASE,
)

# ─── Постобробка артефактів, які залишаються після видалення слів/фраз ───
# 1) Пробіл перед розділовим знаком: "менеджеру ." → "менеджеру."
#    (залишається після видалення слова перед крапкою, напр. _VIATEC_WORD_DELETE_RE).
_SPACE_BEFORE_PUNCT_RE: re.Pattern = re.compile(r'\s+([.,;:!?])')

# 2) Подвійна/потрійна пунктуація: ".." → "."
#    (виникає, коли видалена фраза сама закінчується крапкою поруч із тією, що йшла далі).
_DUPLICATE_PUNCT_RE: re.Pattern = re.compile(r'([.,;:!?])\1+')

# 3) Відсутній пробіл після крапки: "Текст.Далі" → "Текст. Далі"
#    (виникає, коли видалена ціла речення-фраза разом із роздільниками з обох боків, і дві
#    речення з'єднуються без роздільного пробілу). У lookahead лише літери (без цифр),
#    щоб не розбивати десяткові числа типу "3.6" чи коди моделей.
_MISSING_SPACE_AFTER_PUNCT_RE: re.Pattern = re.compile(
    r'([.,;:!?])(?=[A-Za-zА-Яа-яЁёІіЇїЄєҐґ])'
)

# Промо-артефакти Prom-розмітки що видаляються повністю.
# Включає попередній роздільник ([,;\s]*) і хвостові пробіли,
# щоб не залишати пунктуаційних артефактів у тексті.
_PROMO_ARTIFACTS: list[str] = [
    "Є товари з аналогічними характеристиками →",
]

_PROMO_ARTIFACTS_RE: re.Pattern = re.compile(
    r'[,;\s]*(?:' + '|'.join(re.escape(p) for p in _PROMO_ARTIFACTS) + r')\.?\s*',
    re.IGNORECASE,
)


# ─── Нормалізація "сирих" whitespace-артефактів джерела ──────────────────────
# У деяких фідах між словами трапляється справжній non-breaking space (U+00A0)
# замість звичайного " " — literal-фрази нижче (LP/Viatec), побудовані через
# re.escape із звичайним пробілом, через це НЕ співпадають і лишаються в описі
# цілком. Нормалізується ДО будь-яких supplier-специфічних видалень.
_NBSP_CHAR_RE: re.Pattern = re.compile('\xa0')


# ─── Загальне для ВСІХ постачальників: "осиротілі" HTML-entity ───────────────
# У фіді деяких постачальників (GreenVision/LP) HTML-entity закодовані з комою
# замість крапки з комою (&nbsp,, &laquo,, &ndash, тощо). Якщо конкретна фраза
# з такою entity не збіглася цілком (напр. великий промо-блок вище) — сама
# entity лишається сиротою в тексті. Прибирається для ВСІХ постачальників,
# незалежно від supplier — сенсового навантаження в описі товару не несе.
#
# Два окремих правила, бо наслідок видалення різний:
#   1) _HTML_ENTITY_DELETE_RE — "невидимі"/парні entity (пробіл, лапки-обгортки,
#      amp/quot тощо) — видаляються ПОВНІСТЮ (порожній рядок), бо самі по собі
#      не несуть візуального розділення між словами.
#   2) _HTML_ENTITY_DASH_RE   — &ndash / &mdash — це візуальне тире-роздільник
#      МІЖ словами (напр. "завода &ndash, виробника", "Ціна&ndash;100 грн").
#      Видалення ПОВНІСТЮ злило б сусідні слова/числа в одне ("Ціна100 грн"),
#      тож замінюється на ПРОБІЛ, а не на порожній рядок.
_HTML_ENTITY_DELETE_RE: re.Pattern = re.compile(
    r'&(?:nbsp|laquo|raquo|amp|quot|hellip|copy|reg)\s*[,;]?',
    re.IGNORECASE,
)

_HTML_ENTITY_DASH_RE: re.Pattern = re.compile(
    r'\s*&(?:ndash|mdash)\s*[,;]?\s*',
    re.IGNORECASE,
)


# ─── Загальне для ВСІХ постачальників: термінологічні заміни слів ───
# На відміну від supplier-специфічних _LP_REPLACE_RULES / _VIATEC_REPLACE_RULES,
# ці заміни застосовуються до ВСІХ товарів, незалежно від переданого
# `supplier` (аналогічно до _HTML_ENTITY_DELETE_RE вище).
#
# "шлюб" → "брак": укр. слово "шлюб" у контексті описів товару вживається
# як виробничий термін для бракованої/дефектної продукції (напр. "відсівати
# шлюб"), але омонімно збігається зі значно поширенішим значенням "одруження" —
# щоб уникнути неоднозначності, замінюється на однозначніше "брак".
# \b…\b — ціле слово (не чепає відміни типу "шлюбу"/"шлюбом").
# Заміна виконується через _replace_preserve_case(), щоб "Шлюб" (з великої
# літери, напр. на початку речення) ставало "Брак", а не "брак" з маленької.
_GENERAL_REPLACE_RULES: list[tuple[str, str]] = [
    ("шлюб", "брак"),
]

_GENERAL_REPLACE_RE: list[tuple[re.Pattern, str]] = [
    (re.compile(r'\b' + re.escape(word) + r'\b', re.IGNORECASE), repl)
    for word, repl in _GENERAL_REPLACE_RULES
]


def _replace_preserve_case(match: re.Match, replacement: str) -> str:
    """
    Замінює збіг на `replacement`, зберігаючи регістр першої літери
    оригіналу (напр. "Шлюб" → "Брак", "шлюб" → "брак"). Використовується
    як `repl` в re.sub для _GENERAL_REPLACE_RE.

    Args:
        match: об'єкт збігу re.Match
        replacement: текст заміни (нижнім регістром)

    Returns:
        `replacement` з регістром першої літери, як у оригіналі
    """
    matched = match.group(0)
    if matched[:1].isupper():
        return replacement[:1].upper() + replacement[1:]
    return replacement

# ─── Загальне для ВСІХ постачальників, але Лише в ОПИСАх ───
# Формат-фікс: окреме слово "COM" (порт/інтерфейс) має завершуватися дефісом —
# лише в описах (не в назві/пошукових запитах), тож це винесено окремо від
# загальних sanitize()-кроків і застосовується окремо, лише до полів з
# TextSanitizer.DESCRIPTION_FIELDS (див. sanitize_item).
#
# Єдине правило, case-insensitive, з нормалізацією кириличних "омографів" —
# С/О/М (U+0421/U+041E/U+041C) та с/о (U+0441/U+043E) візуально невідрізнювані
# від латинських C/O/M/c/o, але мають інший codepoint і тому НЕ збігаються з
# латинським regex навіть при IGNORECASE (кирилиця/латиниця — різні
# case-folding таблиці). Типова причина: оператор фіда не встиг перемкнути
# розкладку клавіатури перед технічним терміном усередині кириличного тексту
# (напр. "Com", де перша літера — кирилична С).
#
# Щоб не чіпати легітимні кириличні слова (напр. "сом" — риба), нормалізація
# кирилиці в латиницю відбувається ТІЛЬКИ якщо у збігу є хоча б одна латинська
# літера (тобто це вже змішане написання, а не чисто кириличне слово).
# \b — ціле слово, аби не чепати підрядок у інших словах (напр. "recommend", "company").
_CYRILLIC_COM_LOOKALIKES: dict[str, str] = {
    "С": "C", "с": "c",
    "О": "O", "о": "o",
    "М": "M",  # лише велика — мала "м" візуально відрізняється від латинської "m"
}
_DESC_ONLY_COM_RE: re.Pattern = re.compile(
    r'\b[CcСс][OoОо][MmМ]\b'
)


def _fix_com_word(match: re.Match) -> str:
    """
    Callback для _DESC_ONLY_COM_RE: додає дефіс після "COM"/"Com"/"com" у
    будь-якому регістрі, нормалізуючи кириличні омографи (С/О/М) до латиниці —
    але лише якщо збіг змішаний (є хоча б одна латинська літера). Чисто
    кириличні збіги (напр. "сом" — риба) лишаються без змін.

    Args:
        match: об'єкт збігу re.Match від _DESC_ONLY_COM_RE

    Returns:
        Нормалізоване слово з дефісом, або оригінал без змін (чиста кирилиця)
    """
    word = match.group(0)
    if all(ch in _CYRILLIC_COM_LOOKALIKES for ch in word):
        return word
    return "".join(_CYRILLIC_COM_LOOKALIKES.get(ch, ch) for ch in word) + "-"


# ─── Загальне для ВСІХ постачальників, але Лише в ОПИСАх: гарантійний строк ──
# У вільному тексті опису іноді дублюється речення/фраза "Гарантія – 6 міс."
# (укр.) або "Гарантия – 18 мес." (рос.), що повторює структуроване поле
# характеристик "Гарантія" — таке дублювання в описі зайве і видаляється.
# ВАЖЛИВО: характеристики товару — окреме поле поза TextSanitizer.
# SANITIZED_FIELDS, це правило їх у принципі не зачіпає; застосовується лише
# до TextSanitizer.DESCRIPTION_FIELDS через _apply_desc_only_rules (див.
# sanitize_item), так само як і COM-правило вище.
#
# Кількість місяців довільна (6, 12, 18, 24 тощо) — \d+, без хардкоду.
# Дефіс/тире між "Гарантія" і числом варіюється в джерелах (звичайний "-",
# короткий "–" U+2013, довгий "—" U+2014) — усі три враховані.
# "міс"/"мес" — укр./рос. скорочення слова "місяців"/"месяцев", крапка після
# скорочення опціональна. Провідний [,;\s]* / кінцевий \.?\s* — той самий
# підхід, що й у _PROMO_ARTIFACTS_RE / _VIATEC_DELETE_RE — щоб не лишати
# "осиротілих" роздільників там, де фраза йшла в переліку через кому чи
# крапку з комою.
_WARRANTY_PERIOD_RE: re.Pattern = re.compile(
    r'[ \t,;]*(?:[-–—][ \t]*)?'
    r'Гарант(?:ія|ия)(?:\s+(?:виробника|производителя))?'
    r'\s*[-–—:]\s*\d+\s*'
    r'(?:міс(?:яц(?:ів|і|ь))?|мес(?:яц(?:ев|а)?)?)'
    r'(?:\s*\(\s*\d+\s*(?:років|роки|рік|года|год|лет)\s*\))?'
    r'\.?\s*',
    re.IGNORECASE,
)


# ─── Загальне для ВСІХ постачальників: посилання та фото/відео у описах ───────────
# 1) <img ...> — видаляється цілком (теги без окремого закриваючого тегу).
# 2) <iframe ...>...</iframe> — вбудовані відео (напр. YouTube embed) — видаляються цілком разом з вмістом.
# 3) <a ...> / </a> — відкриваючий і закриваючий теги видаляються ОКРЕМО (не як пара),
#    текст/вкладені теги (<strong> тощо) лишаються. Це свідомо, бо в описах трапляються
#    незакриті/непарні <a> (кілька відкриваючих підряд без відповідного </a> поруч) —
#    парний regex (`<a...>(.*?)</a>`) в такому разі просто не знаходить збіг і лишає
#    сирий тег у тексті. Окреме видалення відкриваючого/закриваючого тегів прибирає
#    розмітку незалежно від того, чи вона коректно парна.
#    Працює для будь-яких лапок в атрибутах (" або типографське ″).
_IMG_TAG_RE: re.Pattern = re.compile(r'<img\b[^>]*>', re.IGNORECASE)

_IFRAME_TAG_RE: re.Pattern = re.compile(
    r'<iframe\b[^>]*>.*?</iframe>',
    re.IGNORECASE | re.DOTALL,
)

_A_OPEN_TAG_RE: re.Pattern = re.compile(r'<a\b[^>]*>', re.IGNORECASE)
_A_CLOSE_TAG_RE: re.Pattern = re.compile(r'</a\s*>', re.IGNORECASE)

# 4) [текст](url) — markdown-посилання (зустрічаються в описах Viatec) —
#    розгортаються так само, як і <a>: лишається лише текст.
_MARKDOWN_LINK_RE: re.Pattern = re.compile(r'\[([^\]]+)\]\([^)]*\)')


# ─── LP (LogicPower) supplier: HTML-артефакти описів ────────────────────────
# Заміна: fragment → крапка (напр., вбудований телефон у тексті).
# Видалення: рекламні посилання та абзаци від виробника.
_LP_REPLACE_RULES: list[tuple[str, str]] = [
    (
        # <strong><a href="tel:+380800309988">+38(080)030-99-88</a></strong>  →  .
        r'<strong><a\s+href=["\u2033]tel:\+380800309988["\u2033]>'
        r'\+38\(080\)030-99-88</a></strong>',
        ".",
    ),
    (
        # RU: "На официальном сайте LogicPower Вы можете купить в Украине" → "Купите"
        r'На официальном сайте LogicPower Вы можете купить в Украине',
        "Купите",
    ),
    (
        # UA: "На офіційному сайті LogicPower Ви можете купити в Україні" → "Купуйте"
        r'На офіційному сайті LogicPower Ви можете купити в Україні',
        "Купуйте",
    ),
    (
        # RU: "На официальном сайте LogicPower можно заказать" → "Заказывайте"
        r'На официальном сайте LogicPower можно заказать',
        "Заказывайте",
    ),
    (
        # UA: "На офіційному сайті LogicPower можна замовити" → "Замовляйте"
        r'На офіційному сайті LogicPower можна замовити',
        "Замовляйте",
    ),
    (
        # RU: "На официальном сайте GreenVision можно купить" → "Покупайте"
        r'На официальном сайте GreenVision можно купить',
        "Покупайте",
    ),
    (
        # UA: "На офіційному сайті GreenVision можна купити" → "Купуйте"
        r'На офіційному сайті GreenVision можна купити',
        "Купуйте",
    ),
]

_LP_DELETE_PATTERNS: list[str] = [
    # RU: "Узнать больше информации Вы можете прочитав <a ...><strong>данную статью</strong></a>."
    r'Узнать больше информации Вы можете прочитав\s+<a\s+href=["\u2033][^"\u2033]+["\u2033]>'
    r'<strong>данную статью</strong></a>\.',
    # UA: "Детальнішу інформацію Ви можете отримати з <a ...><strong>нашої статті</strong></a>."
    r'Детальнішу інформацію Ви можете отримати з\s+<a\s+href=["\u2033][^"\u2033]+["\u2033]>'
    r'<strong>нашої статті</strong></a>\.',
    # RU: промо-абзац про замовлення на офіційному сайті
    r'<p>На официальном сайте LogicPower можно заказать аккумуляторные батареи для ИБП'
    r' и другие типы аккумуляторов с доставкой по Украине\.\s*Цена\s*[–\-]\s*напрямую от производителя\.</p>',
    # UA: промо-абзац про замовлення на офіційному сайті
    r'<p>На офіційному сайті LogicPower можна замовити акумуляторні батареї для ДБЖ'
    r' та інші типи акумуляторів з доставкою по Україні\.\s*Ціна\s*[–\-]\s*безпосередньо від виробника\.</p>',
    # RU: повідомлення про відсутність акції безкоштовної доставки (сонячні панелі)
    r'(?:<p>)?На этот товар не распространяется акция по бесплатной доставке\.\s*'
    r'Доставка солнечных панелей осуществляется за счет покупателя согласно тарифам перевозчика\.(?:</p>)?',
    # UA: повідомлення про відсутність акції безкоштовної доставки (сонячні панелі)
    r'(?:<p>)?На цей товар не розповсюджується акція з безкоштовної доставки\.\s*'
    r'Доставлення сонячних панелей здійснюється за рахунок покупця згідно з тарифами перевізника\.(?:</p>)?',
    # RU: про можливість купівлі в кредит/рассрочку (інвертори)
    r'(?:<p>)?На официальном сайте LogicPower можно купить инвертор для солнечных батарей в Украине'
    r' в кредит или воспользовавшись услугой.*?оплата частями.*?\.(?:</p>)?',
    # UA: про можливість купівлі в кредит/розстрочку (інвертори)
    r'(?:<p>)?На офіційному сайті LogicPower можна купити інвертор для сонячних батарей в Україні'
    r' в кредит або скориставшись послугою.*?оплата частинами.*?\.(?:</p>)?',
    # Літеральний img src-фрагмент від logicfox.info (конкретне зображення в описі)
    r'["\u2033]\s*src=["\u2033]https://logicfox\.info/foto/4148/site/4148_content_2\.jpg["\u2033]',
    # RU: блок про заповнення заявки на сайті / умови доставки й оплати
    r'(?:<p>)?Для получения товара заполните заявку на сайте\.\s*'
    r'Сроки и способ доставки, а также форма оплаты заказа оговариваются при оформлении заказа\.(?:</p>)?',
    # UA: блок про заповнення заявки на сайті / умови доставлення й оплати
    r'(?:<p>)?Для отримання товару заповніть заявку на сайті\.\s*'
    r'Терміни й спосіб доставлення, а також форма оплати замовлення обумовлюються при оформленні замовлення\.(?:</p>)?',
    # UA: абзац про вибір способу оплати (передоплата/розстрочка від ПриватБанку) —
    # regex (а не literal-фраза в _LP_DELETE_PHRASES), бо \s+ витримує non-breaking
    # space / подвійні пробіли між словами джерела; крапка на кінці опціональна
    # (в описах трапляється і без неї).
    r'При\s+оформленні\s+замовлення\s+можна\s+вибрати\s+спосіб\s+оплати\s*'
    r'\(передоплата\s+або\s+післяплата\)\s*а\s+також\s+скористатися\s+послугами\s*'
    r'(?:&laquo\s*[,;]?\s*)?миттєва\s+розстрочка(?:&raquo\s*[,;]?\s*)?\s*та\s*'
    r'(?:&laquo\s*[,;]?\s*)?оплата\s+частинами(?:&raquo\s*[,;]?\s*)?\s*'
    r'від\s+ПриватБанку\.?',
    # RU: абзац про выбор способа оплаты (предоплата/рассрочка от ПриватБанка) —
    # той самий підхід, що й UA-версія вище. Квот-маркери &laquo/&raquo ОПЦІОНАЛЬНІ
    # цілком (у частині описів фраза йде взагалі без лапок), а сама entity може
    # завершуватись і комою, і крапкою з комою (&laquo, / &laquo;) — обидва варіанти
    # враховані через [,;]?, як і в загальному _HTML_ENTITY_DELETE_RE.
    r'При\s+оформлении\s+заказа\s+можно\s+выбрать\s+способ\s+оплаты\s*'
    r'\(предоплата\s+или\s+наложенный\s+платеж\)\s*а\s+также\s+воспользоваться\s+услугами\s*'
    r'(?:&laquo\s*[,;]?\s*)?мгновенная\s+рассрочка(?:&raquo\s*[,;]?\s*)?\s*и\s*'
    r'(?:&laquo\s*[,;]?\s*)?оплата\s+частями(?:&raquo\s*[,;]?\s*)?\s*'
    r'от\s+ПриватБанк\.?',
    # RU: інший варіант того самого абзацу — "Вы можете выбрать" замість "можно
    # выбрать", закінчення "ПриватБанка"/"ПриватБанк" обидва варіанти (родовий
    # відмінок після "от" вимагає "-а", але в частині описів кінцівка не додана).
    r'При\s+оформлении\s+заказа\s+вы\s+можете\s+выбрать\s+способ\s+оплаты\s*'
    r'\(предоплата\s+или\s+наложенный\s+плат[её]ж\)\s*,?\s*а\s+также\s+воспользоваться\s+услугами\s*'
    r'(?:&laquo\s*[,;]?\s*)?мгновенная\s+рассрочка(?:&raquo\s*[,;]?\s*)?\s*и\s*'
    r'(?:&laquo\s*[,;]?\s*)?оплата\s+частями(?:&raquo\s*[,;]?\s*)?\s*'
    r'от\s+ПриватБанк[аА]?\.?',
    # RU: короткий перелічувальний варіант "Несколько способов оплаты заказа:" —
    # опційний <strong>-обгортка навколо заголовка (в описах трапляється і без неї).
    r'(?:<strong>)?\s*Несколько\s+способов\s+оплаты\s+заказа\s*:?\s*(?:</strong>)?\s*'
    r'предоплата\s*,\s*оплата\s+на\s+карту\s*,\s*наложенный\s+платеж\s*,\s*а\s+также\s*'
    r'(?:&laquo\s*[,;]?\s*)?мгновенная\s+рассрочка(?:&raquo\s*[,;]?\s*)?\s*и\s*'
    r'(?:&laquo\s*[,;]?\s*)?оплата\s+частями(?:&raquo\s*[,;]?\s*)?\s*'
    r'от\s+ПриватБанк[аА]?\.?',
    # UA: короткий перелічувальний варіант "Декілька способів оплати замовлення:" —
    # той самий підхід, що й RU-версія вище.
    r'(?:<strong>)?\s*Декілька\s+способів\s+оплати\s+замовлення\s*:?\s*(?:</strong>)?\s*'
    r'передоплата\s*,\s*оплата\s+на\s+картку\s*,\s*післяплата\s*,\s*а\s+також\s*'
    r'(?:&laquo\s*[,;]?\s*)?миттєва\s+розстрочка(?:&raquo\s*[,;]?\s*)?\s*та\s*'
    r'(?:&laquo\s*[,;]?\s*)?оплата\s+частинами(?:&raquo\s*[,;]?\s*)?\s*'
    r'від\s+ПриватБанку\.?',
]

_LP_REPLACE_RE: list[tuple[re.Pattern, str]] = [
    (re.compile(pattern, re.IGNORECASE), repl)
    for pattern, repl in _LP_REPLACE_RULES
]

_LP_DELETE_RE: re.Pattern = re.compile(
    r'(?:' + '|'.join(_LP_DELETE_PATTERNS) + r')',
    re.IGNORECASE,
)

# Плейн-текстові LP-фрази для повного видалення. На відміну від _LP_DELETE_PATTERNS
# (ручний regex-синтаксис з екрануванням спецсимволів), тут звичайний текст —
# екранування (re.escape) відбувається автоматично при компіляції нижче. Зручно
# для довгих промо-блоків з HTML-entities (&ndash,, &laquo,, &nbsp, тощо), де ручне
# написання regex ризикує помилками.
_LP_DELETE_PHRASES: list[str] = [
    # RU: коротка фраза про ПЗ/оновлення на сайті GreenVision (варіант "к нему")
    "ПО и обновления к нему можно скачать на официальном сайте компании GreenVision.",
    # RU: коротка фраза про ПЗ/оновлення на сайті GreenVision (варіант без "к нему")
    "ПО и обновления можно скачать на официальном сайте компании GreenVision.",
    # UA: коротка фраза про ПЗ/оновлення на сайті GreenVision
    "ПЗ та оновлення до нього можна завантажити на офіційному сайті компанії GreenVision.",
    # RU: великий промо-блок "переваги GreenVision" (пряма поставка, консультації,
    # монтаж, оплата, доставка) — повторюється у кожному описі товарів GreenVision.
    "Оборудование GreenVision поступает в Украину напрямую с завода &ndash, производителя, что позволяет нам контролировать качество и отсевать брак до того, как товар попадет к потребителю. Компания на правах производителя осуществляет жесткую политику контроля цен, чтобы не допустить неоправданного завышения стоимости нашей продукции.</li> <li><strong>Бесплатное консультирование и помощь в выборе комплекта видеонаблюдения согласно целям и задачам потребителя</strong>. Наши специалисты ответят на все ваши вопросы и помогут подобрать оборудование исходя из практического применения систем видеонаблюдения на различных объектах.</li> <li><strong>Возможность заказать услуги монтажа и настройки оборудования</strong>. Доверьте совою безопасность профессионалам.</li> <li><strong>Возможность выбрать удобный способ оплаты</strong>. При оформлении заказа можно выбрать способ оплаты (предоплата или наложенный платеж) а также воспользоваться услугами &laquo, мгновенная рассрочка&raquo, и &laquo, оплата частями&raquo, от ПриватБанк.</li> <li><strong>Бесплатная доставка.</strong> Доставляем оборудование по всей территории Украины согласно актуальных карт почтовых операторов.&nbsp, Не упускайте свой шанс купить комплект видеонаблюдения в Украине по выгодной цене и возможностью не платить за доставку.</li> <li>",
    # UA: великий промо-блок "переваги GreenVision" (українська версія)
    ".</strong> Обладнання GreenVision надходить в Україну безпосередньо із заводу &ndash, виробника, що дозволяє нам контролювати якість та відсівати шлюб до того, як товар потрапить до споживача. Компанія на правах виробника здійснює жорстку політику контролю цін, щоб запобігти невиправданому завищенню вартості нашої продукції.</p> </li> <li> <p><strong>Безплатне консультування та допомога у виборі комплекту відеоспостереження відповідно до цілей та завдань споживача.</strong> Наші фахівці дадуть відповідь на всі ваші запитання та допоможуть підібрати обладнання виходячи з практичного застосування систем відеоспостереження на різних об&#39, єктах.</p> </li> <li> <p><strong>Можливість замовити послуги монтажу та налаштування обладнання.</strong> Довірте свою безпеку професіоналам.</p> </li> <li> <p><strong>Можливість вибрати зручний спосіб оплати.</strong> При оформленні замовлення можна вибрати спосіб оплати (передоплата або післяплата) а також скористатися послугами &laquo, миттєва розстрочка&raquo, та &laquo, оплата частинами&raquo, від ПриватБанку.</p> </li> <li> <p><strong>Безкоштовна доставка.</strong>&nbsp, Доставляємо обладнання по всій території України згідно з актуальними мапами поштових операторів.&nbsp, Не втрачайте свій шанс купити комплект відеоспостереження за вигідною ціною та можливістю не платити за доставку.</p> </li>",
    # UA: блок про доставку товару по Україні / умови оплати й способу доставки
    "Інтернет-магазин доставляє товар по всій території України. Для отримання товару заповніть заявку на сайті. Форма оплати товару, терміни і спосіб доставки обговорюються з покупцем при оформленні замовлення.",
    # RU: блок про доставку товару по Україні / умови оплати й способу доставки
    "Интернет-магазин доставляет товар по всей территории Украины. Для получения товара заполните заявку на сайте. Форма оплаты товара, сроки и способ доставки оговариваются с покупателем при оформлении заказа.",
    # UA: коротка версія (без "Інтернет-магазин") про заявку на сайті / терміни та спосіб доставки
    "Для отримання товару заповніть заявку на сайті. Терміни та спосіб доставки, а також форма оплати замовлення обумовлюються під час оформлення замовлення.",
    # RU: коротка версія (без "Интернет-магазин", з другим формулюванням/дублюванням "доставки") про заявку на сайте / сроки и способ доставки
    "Для получения товара заполните заявку на сайте. Сроки и способ доставки доставки (мы сотрудничаем со всеми ведущими почтовыми операторами), а так же форма оплаты заказа оговариваются при оформлении заказа.",
    # UA: коротка фраза про замовлення через власний інтернет-магазин GreenVision
    "через наш інтернет-магазин",
    # RU: коротка фраза про заказ через наш интернет-магазин GreenVision
    "через наш интернет-магазин",
    # UA: заголовок-вступ до блоку переваг замовлення на офіційному інтернет-магазині GreenVision
    "Переваги замовлення на офіційному інтернет-магазині GreenVision:",
    # RU: заголовок-вступ до блока преимуществ заказа на официальном интернет-магазине GreenVision
    "Преимущества заказа на официальном интернет-магазине GreenVision:",

    # ─── Окремі фрагменти промо-блоку переваг GreenVision ───
    # Великий блок вище прив’язаний до конкретної HTML-розмітки (<li><p><strong>...).
    # В описах без цієї обгортки самі фрази трапляються — тому додаємо їх як окремі
    # фрагменти (альтернатива в regex найспробує великий блок першим, він вище в списку).
    # UA: абзац про пряму поставку від виробника (без обгортки <li><strong>)
    "Обладнання GreenVision надходить в Україну безпосередньо із заводу &ndash, виробника, що дозволяє нам контролювати якість та відсівати шлюб до того, як товар потрапить до споживача. Компанія на правах виробника здійснює жорстку політику контролю цін, щоб запобігти невиправданому завищенню вартості нашої продукції.",
    # RU: абзац про пряму поставку от завода-производителя (без обгортки <li><strong>)
    "Оборудование GreenVision поступает в Украину напрямую с завода &ndash, производителя, что позволяет нам контролировать качество и отсевать брак до того, как товар попадет к потребителю. Компания на правах производителя осуществляет жесткую политику контроля цен, чтобы не допустить неоправданного завышения стоимости нашей продукции.",
    # UA: коротка фраза-заголовок про безкоштовну доставку
    "Безкоштовна доставка.",
    # RU: короткая фраза-заголовок про бесплатную доставку
    "Бесплатная доставка.",
    # UA: абзац про доставку по території україни (з &nbsp,), без обгортки <li><strong>
    "&nbsp, Доставляємо обладнання по всій території України згідно з актуальними мапами поштових операторів.&nbsp, Не втрачайте свій шанс купити комплект відеоспостереження за вигідною ціною та можливістю не платити за доставку.",
    # RU: абзац про доставку по территории украины (без початкового &nbsp,), без обгортки <li><strong>
    "Доставляем оборудование по всей территории Украины согласно актуальных карт почтовых операторов.&nbsp, Не упускайте свой шанс купить комплект видеонаблюдения в Украине по выгодной цене и возможностью не платить за доставку.",

    # ─── "Хвостові" фрази, що лишаються окремо після часткового видалення ──────
    # великих блоків/фрагментів переваг GreenVision вище (коли решта абзацу вже
    # прибрана іншим правилом, а саме це закінчення — ні).
    # RU: хвіст "...и возможностью не платить за доставку."
    " и возможностью не платить за доставку.",
    # UA: хвіст "...та можливістю не платити за доставку."
    "та можливістю не платити за доставку.",
    # RU: короткий варіант хвоста про безкоштовну доставку — "и без платы за доставку."
    # (відмінний від вже існуючих вище хвостів "и возможностью не платить..." —
    # тут інше формулювання, без слова "возможностью").
    "и без платы за доставку.",
]

_LP_DELETE_PHRASES_RE: re.Pattern = re.compile(
    r'(?:' + '|'.join(re.escape(p) for p in _LP_DELETE_PHRASES) + r')',
    re.IGNORECASE,
)


# ─── Viatec supplier: промо-фрази та артефакти описів ───────────────────────
# Видаляються повністю (разом з попереднім роздільником, як і _PROMO_ARTIFACTS).
_VIATEC_DELETE_PHRASES: list[str] = [
    "Є товари з аналогічними характеристиками →",
    "Есть товары с аналогичными характеристиками →",
    "В компанії Viatec є рішення!",
    "У компании Viatec есть решение!",
    "або реєстрації як партнера Viatec",
    "или регистрации в качестве партнера Viatec",
    "или зарегистрируйтесь как партнер для доступа к эксклюзивным предложениям",
    "або зареєструйтеся як партнер для доступу до ексклюзивних пропозицій",
    "или зарегистрируйтесь как партнер на портале Viatec",
    "або зареєструйтесь як партнер на порталі Viatec",
    # Футерний disclaimer, повторюється майже в кожному описі товару Viatec.
    "Технические характеристики и фото могут быть изменены производителем без предварительного уведомления.",
    "Технічні характеристики та фото можуть бути змінені виробником без попереднього повідомлення.",
]

_VIATEC_DELETE_RE: re.Pattern = re.compile(
    r'[,;\s]*(?:' + '|'.join(re.escape(p) for p in _VIATEC_DELETE_PHRASES) + r')\.?\s*',
    re.IGNORECASE,
)

# Заміни Viatec (виконуються після видалень, до видалення слів Viatec/Акція).
_VIATEC_REPLACE_RULES: list[tuple[str, str]] = [
    ("до вашого менеджера", "до менеджера"),
    ("Viatec пропонує", "Пропонуєм"),
    ("Viatec предлагает", "Предлагаем"),
]

_VIATEC_REPLACE_RE: list[tuple[re.Pattern, str]] = [
    (re.compile(re.escape(pattern), re.IGNORECASE), repl)
    for pattern, repl in _VIATEC_REPLACE_RULES
]

# Viatec: заголовок короткого блоку перед повним описом. Заміна діє лише на
# поля Опис/Опис_укр: у назвах товарів і пошукових запитах цей текст не є
# артефактом, тому там його змінювати не можна.
_VIATEC_DESCRIPTION_REPLACE_RULES: list[tuple[str, str]] = [
    ("Ключевые характеристики", "Описание"),
    ("Ключові характеристики", "Опис"),
]

_VIATEC_DESCRIPTION_REPLACE_RE: list[tuple[re.Pattern, str]] = [
    (re.compile(re.escape(pattern), re.IGNORECASE), repl)
    for pattern, repl in _VIATEC_DESCRIPTION_REPLACE_RULES
]

# Фінальне видалення окремих слів для Viatec — після всіх інших кроків.
# Слова видаляються цілком (не замінюються пробілом, як BANNED_WORDS) —
# це слова без якої сенсової цінності окремо від решти тексту.
_VIATEC_WORD_DELETE: list[str] = [
    "Viatec",
    "Акція",
    "Акция",
    "Акции",
    "Акції",
]

_VIATEC_WORD_DELETE_RE: re.Pattern = re.compile(
    r'\b(?:' + '|'.join(re.escape(w) for w in _VIATEC_WORD_DELETE) + r')\b',
    re.IGNORECASE,
)


# ─── Secur supplier: фрази для повного видалення (усі SANITIZED_FIELDS) ────
# Аналог _LP_DELETE_PHRASES / _VIATEC_DELETE_PHRASES — звичайний текст,
# екранування (re.escape) відбувається автоматично при компіляції нижче.
# Наразі порожній: жодної secur-специфічної промо-фрази ще не виявлено.
# Порожній список — свідомий безпечний no-op (див. guard у sanitize():
# _SECUR_DELETE_RE == None, доки сюди не додано хоча б одну фразу).
_SECUR_DELETE_PHRASES: list[str] = []

_SECUR_DELETE_RE: re.Pattern | None = (
    re.compile(
        r'(?:' + '|'.join(re.escape(p) for p in _SECUR_DELETE_PHRASES) + r')',
        re.IGNORECASE,
    )
    if _SECUR_DELETE_PHRASES
    else None
)


# ─── Secur supplier: назва товару має починатися з виду/типу товару ────────
# Rozetka-модерація банить товари, назва яких починається не з виду/типу
# товару (напр. "Ключ-брелок", "Безконтактна картка"), а з кількісного
# префікса "Набір/Набор <N> шт." (де <N> — довільна кількість: 25, 75, 100,
# 500 тощо). Приклад:
#   "Набір 25 шт. Безконтактна картка Tecsar Trek EM-Marine 1,6 мм біла
#    з прорізом Tecsar 3081"
#   → "Безконтактна картка Tecsar Trek EM-Marine 1,6 мм біла з прорізом
#      Tecsar 3081. Набір 25 шт"
# Префікс переноситься в кінець назви (після виду/типу товару), а не
# видаляється — кількість у наборі важлива для покупця і має лишитись.
#
# Правило застосовується ЛИШЕ до полів назви (TextSanitizer.TITLE_FIELDS) і
# ЛИШЕ коли supplier == "secur" (див. sanitize_item) — на відміну від
# _SECUR_DELETE_PHRASES, які застосовуються в sanitize() до всіх
# SANITIZED_FIELDS. Винесено окремо, бо стосується виключно структури
# назви, а не очищення тексту від небажаного вмісту.
#
# "шт" в джерелі завжди супроводжується крапкою — ця крапка є надійним
# роздільником між префіксом і рештою назви, тож використовується як якір.
_SECUR_NAME_QTY_PREFIX_RE: re.Pattern = re.compile(
    r'^(?P<prefix>(?:Набір|Набор)\s+\d+\s*шт)\.\s*(?P<rest>.+)$',
    re.IGNORECASE,
)


def _move_secur_qty_prefix_to_end(text: str) -> str:
    """
    Переносить кількісний префікс "Набір/Набор <N> шт." з початку назви
    товару Secur в кінець (після виду/типу товару) — вимога модерації
    Rozetka: назва має починатися з виду/типу товару.

    Кількість <N> довільна (25, 75, 100, 500 тощо) — не хардкодиться,
    визначається регексом _SECUR_NAME_QTY_PREFIX_RE.

    Args:
        text: назва товару (вже після sanitize())

    Returns:
        Назву з перенесеним у кінець кількісним префіксом. Якщо префікс не
        знайдено на початку рядка (або решта назви порожня) — оригінал
        повертається без змін, безпечний fallback, щоб не ламати назви
        іншого формату чи не отримати порожню назву.
    """
    if not text:
        return text
    match = _SECUR_NAME_QTY_PREFIX_RE.match(text)
    if not match:
        return text
    prefix = match.group("prefix").strip()
    rest = match.group("rest").strip().rstrip(".").strip()
    if not rest:
        return text
    return f"{rest}. {prefix}"


class TextSanitizer:
    """Очищення текстових полів від заборонених слів"""

    # Поля товару, які підлягають перевірці
    SANITIZED_FIELDS: tuple[str, ...] = (
        "Назва_позиції",
        "Назва_позиції_укр",
        "Опис",
        "Опис_укр",
        "Пошукові_запити",
        "Пошукові_запити_укр",
    )

    # Поля-описи, до яких (і лише до яких) додатково застосовується
    # _DESC_ONLY_REPLACE_RE (COM → COM-, com → com-). Однакове для ВСІХ
    # постачальників — supplier тут не має значення.
    DESCRIPTION_FIELDS: tuple[str, ...] = (
        "Опис",
        "Опис_укр",
    )

    # Поля-назви, до яких (і лише до яких) додатково застосовується
    # перенесення кількісного префікса "Набір/Набор N шт." в кінець назви
    # (див. _move_secur_qty_prefix_to_end) — і лише коли supplier == "secur"
    # (див. sanitize_item). Не застосовується до описів/пошукових запитів —
    # там префікс на початку не порушує вимог модерації Rozetka.
    TITLE_FIELDS: tuple[str, ...] = (
        "Назва_позиції",
        "Назва_позиції_укр",
    )

    @staticmethod
    def sanitize(text: str, supplier: str | None = None) -> str:
        """
        Видаляє промо-артефакти, посилання/фото та замінює заборонені слова на пробіл.

        Порядок:
          1. LP-специфічні видалення/заміни      — лише коли supplier == "lp".
          2. Viatec-специфічні видалення/заміни  — лише коли supplier == "viatec".
          3. Secur-специфічні видалення       — лише коли supplier == "secur".
          4. _IMG_TAG_RE / _IFRAME_TAG_RE / _A_OPEN_TAG_RE+_A_CLOSE_TAG_RE / _MARKDOWN_LINK_RE
             — видалення <img>/<iframe> та відкриваючого/закриваючого <a>-тегу окремо
             та [текст](url) — для ВСІХ постачальників.
          5. _PROMO_ARTIFACTS_RE                — видалення загальних промо-фраз разом з роздільником.
          6. _HTML_ENTITY_DELETE_RE / _HTML_ENTITY_DASH_RE — видалення осиротілих HTML-entity (&nbsp,, &laquo, — повністю; &ndash/&mdash — на пробіл) — для ВСІХ постачальників.
          7. _GENERAL_REPLACE_RE                — термінологічні заміни окремих слів (напр. шлюб → брак) — для ВСІХ постачальників, з збереженням регістру.
          8. _VIATEC_WORD_DELETE_RE             — видалення слів Viatec/Акція — лише коли supplier == "viatec", в самому кінці.
          9. _BANNED_PATTERN                    — заміна заборонених слів на пробіл.
          10. Collapse зайвих пробілів.

        Args:
            text: вхідний рядок
            supplier: нормалізоване ім'я постачальника (SupplierConfig.supplier_name,
                напр. "lp", "viatec"). None/інше значення — supplier-специфічні
                кроки пропускаються.

        Returns:
            Очищений рядок
        """
        if not text:
            return text

        # Нормалізація non-breaking space ДО будь-яких supplier-специфічних
        # видалень — інакше literal-фрази (LP/Viatec) не збігаються, якщо
        # джерело використало U+00A0 замість звичайного пробілу.
        cleaned = _NBSP_CHAR_RE.sub(' ', text)

        if supplier == "lp":
            cleaned = _LP_DELETE_RE.sub("", cleaned)
            cleaned = _LP_DELETE_PHRASES_RE.sub("", cleaned)
            for pattern, repl in _LP_REPLACE_RE:
                cleaned = pattern.sub(repl, cleaned)
        elif supplier == "viatec":
            cleaned = _VIATEC_DELETE_RE.sub("", cleaned)
            for pattern, repl in _VIATEC_REPLACE_RE:
                cleaned = pattern.sub(repl, cleaned)
        elif supplier == "secur" and _SECUR_DELETE_RE is not None:
            cleaned = _SECUR_DELETE_RE.sub("", cleaned)

        # Загальне для ВСІХ постачальників: видалення фото/відео та розгортання посилань
        cleaned = _IMG_TAG_RE.sub("", cleaned)
        cleaned = _IFRAME_TAG_RE.sub("", cleaned)
        cleaned = _A_OPEN_TAG_RE.sub("", cleaned)
        cleaned = _A_CLOSE_TAG_RE.sub("", cleaned)
        cleaned = _MARKDOWN_LINK_RE.sub(r'\1', cleaned)

        cleaned = _PROMO_ARTIFACTS_RE.sub("", cleaned)

        # Загальне для ВСІХ постачальників: прибирання "осиротілих" HTML-entity
        # (&nbsp,, &laquo, тощо), що не входили цілком до жодної видаленої фрази.
        # &ndash/&mdash — окремо, бо це візуальне тире-роздільник між словами: замінюється
        # на пробіл, а не видаляється повністю — інакше сусідні слова/числа злилися б в одне.
        cleaned = _HTML_ENTITY_DELETE_RE.sub("", cleaned)
        cleaned = _HTML_ENTITY_DASH_RE.sub(" ", cleaned)

        # Загальне для ВСІХ постачальників: термінологічні заміни окремих слів
        # (напр. шлюб → брак), з збереженням регістру першої літери.
        for pattern, repl in _GENERAL_REPLACE_RE:
            cleaned = pattern.sub(lambda m, r=repl: _replace_preserve_case(m, r), cleaned)

        if supplier == "viatec":
            cleaned = _VIATEC_WORD_DELETE_RE.sub("", cleaned)

        cleaned = _BANNED_PATTERN.sub(" ", cleaned)

        # Постобробка артефактів після видалень вище (слова/фрази):
        # пробіл перед розділовим знаком, подвійна пунктуація,
        # відсутній пробіл після крапки/коми між двома реченнями.
        cleaned = _SPACE_BEFORE_PUNCT_RE.sub(r'\1', cleaned)
        cleaned = _DUPLICATE_PUNCT_RE.sub(r'\1', cleaned)
        cleaned = _MISSING_SPACE_AFTER_PUNCT_RE.sub(r'\1 ', cleaned)

        # Прибираємо подвійні пробіли що могли утворитися
        return re.sub(r' {2,}', ' ', cleaned).strip()

    @classmethod
    def sanitize_item(cls, item: dict, supplier: str | None = None) -> dict:
        """
        Очищає всі текстові поля товару in-place.

        Args:
            item: словник полів товару (result з _clean_item)
            supplier: нормалізоване ім'я постачальника (напр. config.supplier_name) —
                вмикає LP/Viatec/Secur-специфічні правила в sanitize(), а також
                перенесення кількісного префікса в назвах (лише supplier == "secur").

        Returns:
            Той самий словник з очищеними полями
        """
        for field in cls.SANITIZED_FIELDS:
            if item.get(field):
                item[field] = cls.sanitize(item[field], supplier=supplier)
                if field in cls.DESCRIPTION_FIELDS:
                    item[field] = cls._apply_desc_only_rules(item[field])
                    if supplier == "viatec":
                        item[field] = cls._apply_viatec_description_rules(item[field])
                if field in cls.TITLE_FIELDS and supplier == "secur":
                    item[field] = cls._apply_secur_title_rules(item[field])
        return item

    @staticmethod
    def _apply_desc_only_rules(text: str) -> str:
        """
        Застосовує правила, що діють лише для полів опису (TextSanitizer.
        DESCRIPTION_FIELDS), однакові для ВСІХ постачальників:
          1. "COM" → "COM-" (див. _DESC_ONLY_COM_RE / _fix_com_word) —
             case-insensitive, з нормалізацією кириличних омографів С/О/М.
          2. Видалення гарантійного строку "Гарантія – N міс." / "Гарантия –
             N мес." (див. _WARRANTY_PERIOD_RE) — дублює структуроване поле
             характеристик "Гарантія", яке це правило НЕ зачіпає (окреме
             поле поза SANITIZED_FIELDS, сюди взагалі не потрапляє).

        Після видалення гарантійного строку прибираються ті самі артефакти
        (пробіл перед розділовим знаком, подвійна пунктуація, відсутній
        пробіл після крапки/коми між реченнями, подвійні пробіли), що й
        наприкінці sanitize() — бо це видалення відбувається вже ПІСЛЯ того,
        як sanitize() відпрацював свою постобробку.

        Args:
            text: вже очищений (після sanitize) текст опису

        Returns:
            Текст з застосованими COM- та warranty-правилами
        """
        if not text:
            return text
        cleaned = _DESC_ONLY_COM_RE.sub(_fix_com_word, text)
        cleaned = _WARRANTY_PERIOD_RE.sub("", cleaned)
        cleaned = _SPACE_BEFORE_PUNCT_RE.sub(r'\1', cleaned)
        cleaned = _DUPLICATE_PUNCT_RE.sub(r'\1', cleaned)
        cleaned = _MISSING_SPACE_AFTER_PUNCT_RE.sub(r'\1 ', cleaned)
        return re.sub(r' {2,}', ' ', cleaned).strip()

    @staticmethod
    def _apply_viatec_description_rules(text: str) -> str:
        """Застосовує Viatec-специфічні заміни лише до полів опису."""
        for pattern, replacement in _VIATEC_DESCRIPTION_REPLACE_RE:
            text = pattern.sub(replacement, text)
        return text

    @staticmethod
    def _apply_secur_title_rules(text: str) -> str:
        """
        Переносить кількісний префікс "Набір/Набор N шт." з початку назви
        товару Secur в кінець (див. _move_secur_qty_prefix_to_end) — щоб
        назва починалася з виду/типу товару, як цього вимагає модерація
        Rozetka. Застосовується лише до TextSanitizer.TITLE_FIELDS і лише
        для supplier == "secur" (див. sanitize_item).

        Args:
            text: вже очищена (після sanitize) назва товару

        Returns:
            Назву з перенесеним у кінець кількісним префіксом, або оригінал
            без змін, якщо префікс не знайдено на початку рядка.
        """
        if not text:
            return text
        return _move_secur_qty_prefix_to_end(text)
