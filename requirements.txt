# Core scraping
# scrapy>=2.18 обов'язково: pipeline hooks без аргументу spider підтримуються
# тільки з 2.18+. На старіших версіях Scrapy передає spider позиційним аргументом.
# async def start() також потребує Scrapy 2.13+.
scrapy>=2.18,<3.0
scrapy-playwright>=0.0.45,<1.0

# HTTP / parsing
requests>=2.31
lxml>=5.0
brotli
brotlicffi>=1.0.0

# Browser automation (noindex script)
playwright>=1.44

# Anti-bot / Cloudflare bypass
playwright-stealth>=1.0.0

# Env / credentials
python-dotenv>=1.0

# Data processing
pandas>=2.0
itemadapter>=0.7
openpyxl>=3.1

# Images
Pillow>=12.2.0
