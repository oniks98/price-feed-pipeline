# Core scraping
# scrapy>=2.13 обов'язково: async def start() (заміна start_requests) підтримується тільки з 2.13+.
# На старіших версіях start() просто не викликається — павук відкривається і одразу закривається
# з 0 товарів, без жодної помилки (стосується всіх пауків: lp_api, viatec_dealer, secur_feed).
scrapy>=2.13
scrapy-playwright>=0.0.34

# HTTP / parsing
requests>=2.31
lxml>=5.0
brotli
brotlicffi>=1.0.0

# Browser automation (noindex script)
playwright>=1.44

# Anti-bot / Cloudflare bypass
playwright-stealth>=1.0.0

# Env / credentials
python-dotenv>=1.0

# Data processing
pandas>=2.0
itemadapter>=0.7
openpyxl>=3.1

# Images
Pillow>=12.2.0